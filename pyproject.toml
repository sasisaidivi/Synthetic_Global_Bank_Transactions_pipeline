[project]
name = "synthetic-global-bank-transactions-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "airflow-operators>=0.11.0",
    "apache-airflow>=3.3.1",
    "apache-airflow-providers-google>=22.4.0",
    "google-cloud-bigquery>=3.45.0",
    "google-cloud-dataproc>=5.30.0",
    "google-cloud-storage>=3.13.1",
    "pyspark>=4.2.0",
]

[project.scripts]
synthetic-global-bank-transactions-pipeline = "synthetic_global_bank_transactions_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
